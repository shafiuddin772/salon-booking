
server:
  port: 5004

spring:
  application:
    name: service-offering

  datasource:
    url: jdbc:mysql://localhost:3306/servicesdb_salon
    username: root
    password: Shafiuddin@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8070/eureka/